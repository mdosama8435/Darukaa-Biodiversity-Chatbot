[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts = -v --tb=short -p no:faulthandler
markers =
    unit: Isolated unit tests that do not depend on external databases
    integration: Real PostgreSQL and pgvector integration tests
